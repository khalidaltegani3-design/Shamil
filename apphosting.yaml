# Minimal Firebase App Hosting Configuration
# This is a stripped-down version to resolve build issues

runConfig:
  maxInstances: 1

# Only essential Firebase environment variables
env:
  NEXT_PUBLIC_FIREBASE_PROJECT_ID: "zoliapp-lite"
  NODE_ENV: "production"
